{
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "cd backend && mvn clean install -DskipTests"
    },
    "start": {
        "command": "cd backend && mvn spring-boot:run"
    }
}
