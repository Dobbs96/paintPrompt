spring.datasource.url=jdbc:postgresql://metro.proxy.rlwy.net:44387/railway
spring.datasource.username=postgres
spring.datasource.password=PBoTMymFfHUrxGOylZfUkUzaPSZvOHvI
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

management.endpoints.web.exposure.include=logfile,health
management.endpoint.logfile.enabled=true
logging.file.name=logs/spring.log

server.port=${PORT:8080}

# Add api key for local host
openai.api.key=${MYAPP_API_KEY}

# Add s3 bucket here for local host
aws.s3.bucket=paintprompt
aws.s3.region=us-east-2
aws.s3.urlPrefix=https://paintprompt.s3.us-east-2.amazonaws.com/
aws.accessKeyId=${S3_ID}
aws.secretKey=${S3_SECRETKEY}

jwt.secret=${JWTSEC}
jwt.expirationMs=14400000